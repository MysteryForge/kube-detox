rules:
  - id: sc-001
    name: "No Default StorageClass"
    description: "Ensure storage classes are explicit."
    target: "StorageClass"
    field: "metadata.annotations.storageclass\.kubernetes\.io/is-default-class"
    expected: "false"
    severity: "warn"
