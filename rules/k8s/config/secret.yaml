rules:
  - id: sec-001
    name: "No Opaque Secrets"
    description: "Prefer CSI driver or vault for secrets, avoid Opaque."
    target: "Secret"
    field: "type"
    expected: "Opaque"
    severity: "warn"
